Use the attached Gemini MailerLite handoff documents as the technical source of truth for this task.

Important:
- The MailerLite API token is already available in Replit Secrets as MAILERLITE_API_TOKEN.
- The MailerLite group ID is already available in Replit Secrets as MAILERLITE_GROUP_ID.
- Do not expose secrets in frontend code.
- Do not hardcode tokens in client-side JavaScript or public files.
- Use server-side code only for MailerLite API requests.

Your job is to implement the MailerLite website signup system inside this Replit project based on the handoff docs.

Build the following:

1. A backend/server-side subscribe endpoint that:
- accepts name + email
- validates input
- sends the subscriber to the MailerLite group using the server-side token
- supports double opt-in using the handoff spec
- returns safe success/error responses

2. A homepage inline signup form
- fields: name, email
- premium clean UX
- success and error states

3. A footer signup form
- fields: name, email
- same backend endpoint
- same UX behavior

4. reCAPTCHA support if feasible in this stack
- if implemented, keep secret keys server-side
- if not implemented yet, leave a clear TODO with exact instructions

5. Documentation
Create or update:
- .env.example
- README_MAILERLITE_INTEGRATION.md

The README must explain:
- what was built
- which secrets are required
- how the endpoint works
- how to test the signup flow
- what still must be done manually in MailerLite dashboard
- what still must be done manually in Namecheap

Also include a beginner-friendly Namecheap DNS walkthrough:
1. how to open Namecheap Advanced DNS
2. how to compare the MailerLite authentication records with the existing Namecheap records
3. which record types I should expect
4. how to tell if a record is missing or mismatched
5. what to do after adding/changing the records
6. how to return to MailerLite and verify/check domain authentication
7. what propagation delay means and when to wait vs when to troubleshoot

6. Use the Gemini handoff docs for:
- group ID
- field mapping
- request body structure
- success/error/confirmation messages
- automation assumptions
- testing expectations

7. Safety rules
- Never print or expose the secret token in logs or frontend responses
- Never commit a real .env file
- Use environment variables only

8. Final output
When done, tell me:
- what files/components/routes you created or changed
- what still must be done manually in MailerLite
- what still must be done manually in Namecheap
- how I should test the full signup flow.  

Important implementation instruction:
- Integrate the signup system into the existing website files and current site structure.
- Do not create isolated demo components unless absolutely necessary.
- Find the real homepage component/template and add the homepage inline signup form there.
- Find the real footer component/template and add the footer signup form there.
- Wire the forms into the actual website flow so they are ready to use in the live site codebase.
- If you cannot determine the correct homepage or footer file, stop and tell me exactly which files need clarification.